console.log("This is tutorial 69 and we are going to know about npm");


// First write npm init
// It takes details for creating the package.json
// Package.json contains the details of dependencies and devDependencies in a project
// package name, version, description, entry point, keyword, author


// npm is a package manager where there are many packages that are build
// npm install slugify  - npm install or npm i to install packages
// Delete node_modules and to recover all modules write npm install


// devDependencies  --->>   npm intall nodemon --save-dev

// A dependency is a library that a project needs to function effectively. 
// DevDependencies are the packages a developer needs during development.

// ~version “Approximately equivalent to version”, will update you to all future patch versions, 
// without incrementing the minor version. ~1.2.3 will use releases from 1.2.3 to <1.3.0.

// ^version “Compatible with version”, will update you to all future minor/patch versions, 
// without incrementing the major version. ^2.3.4 will use releases from 2.3.4 to <3.0.0.

// >version	Must be greater than version